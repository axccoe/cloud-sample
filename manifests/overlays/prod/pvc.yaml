apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cloud-sample-pvc
  namespace: 82188379-cloud-sample
spec:
  storageClassName: default
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi